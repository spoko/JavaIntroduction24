package poly;

public interface Senior {
    //all methods in the interface are without a body:
    void helpOthers();//public by default, and it cannot be anything else!
    String giveFeedback();//public by default, and it cannot be anything else!
}
